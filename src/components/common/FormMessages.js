/**
 * This is the messages used to inform the user throughout the app.
 * Keeping them in the same file, makes it easier to ensure they all have a suitable tone of voice.
 */

// Message about the image URL
export const urlMessage =
    '(Make sure the URL links to a publicly accessible image.)'
